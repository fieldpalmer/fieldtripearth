// holds the context object
// imported into context_state_config.js
// will be imported into each useContext() hooked component
import React from "react";

const Context = React.createContext();

export default Context;
